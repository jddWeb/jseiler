########################################################
# User.tsconfig – Benutzerspezifische Backend-Einstellungen
########################################################

# Zeigt Seiten-IDs im Seitenbaum
options.pageTree.showPageIdWithTitle = 1

# Aktiviert das Admin Panel (Frontend-Debug)
admPanel.enable = 1

# Listenmodul anpassen
mod.web_list.hideTables = be_sessions,sys_history

# Zusätzliche Optionen im Backend
options {
    showDuplicate = 1
    clearCache.all = 1
}
